import java.util.ArrayList;
import java.util.Scanner;

public class SmartPlanning {

    public static Scanner scanner = new Scanner(System.in);
    /*
    everything related to "smart planning" functions. Everything here should probably
    be static.

    SMART PLANNING:
    uses a formula based on duration and priority (and dueDate later on). What should the ratio be?
    Duration should weigh heavier than priority, because it's more important to fill your time slot
    with as big a task as possible, rather than a high priority but tiny task.
     */

    public static ArrayList<Category> getLists(){ // get the lists that you want "smart plan"
        // to choose tasks from

        return null;

    }

}